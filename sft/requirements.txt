# SFT-only dependencies (HF Trainer + LoRA + optional multi-GPU DDP).
# Install:  pip install -r requirements.txt
#
# Do NOT install DeepSpeed in this venv: accelerate may import it when unwrapping PEFT and then
# DeepSpeed requires CUDA_HOME at import time. GRPO leftovers:  pip uninstall -y deepspeed
#
torch>=2.4.0
transformers>=4.51.0,<6
accelerate>=0.34.0
datasets>=2.20.0
peft>=0.12.0
safetensors>=0.4.3
pyyaml>=6.0
Pillow>=10.0.0
numpy>=1.23
sentencepiece>=0.1.99
einops>=0.8.0
packaging>=23.0
